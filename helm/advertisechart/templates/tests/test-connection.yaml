apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "advertisechart.fullname" . }}-test-connection"
  labels:
    {{- include "advertisechart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "advertisechart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
